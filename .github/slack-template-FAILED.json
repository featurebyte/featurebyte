{
  "channel": "${AUTHOR_ID}",
  "text": "🔴 Failed Action ${GITHUB_WORKFLOW} for ${REPOSITORY}:${GITHUB_HEAD_REF}",
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "🔴 *Failed Action* `${GITHUB_WORKFLOW}` for `${REPOSITORY}`:`${GITHUB_HEAD_REF}`\n*Coverage:* `${COVERAGE_PERCENTAGE}`"
      },
      "accessory": {
        "type": "button",
        "text": {
          "type": "plain_text",
          "text": "Run",
          "emoji": true
        },
        "style": "danger",
        "value": "btn-run",
        "url": "https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
      }
    }
  ]
}
