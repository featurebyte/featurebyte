profile:
  - name: featurebyte1
    api_url: https://app1.featurebyte.com/api/v1
    api_token: API_TOKEN_VALUE1
  - name: featurebyte2
    api_url: https://app2.featurebyte.com/api/v1
    api_token: API_TOKEN_VALUE2

logging:
  level: INFO
  serialize: True

credential:
  - feature_store: "Snowflake FeatureStøre"
    credential_type: USERNAME_PASSWORD
    username: user
    password: password
  - feature_store: other feature store
    credential_type: USERNAME_PASSWORD
    username: random_username
    password: random_password

storage:
  local_path: ~/.featurebyte_custom/data
